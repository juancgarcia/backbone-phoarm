define([
    // Libraries
    'jquery',
    'underscore',
    'backbone',

    // Modules
    './HierarchyNode'

    // Library extensions
],
function($, _, Backbone, HierarchyNodeView){

    var BaseView = HierarchyNodeView.extend({});

    return BaseView;
});